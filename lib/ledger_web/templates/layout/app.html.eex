<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Ledger!</title>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <nav class="top-nav">
          <!-- responsive-->
          <input id="bmenub" type="checkbox" class="show">
          <label for="bmenub" class="burger pseudo button">&#9776;</label>
          <div class="menu">
            <a class="pseudo button" href="<%= page_path(@conn, :index) %>">Dashboard</a>
            <a class="pseudo button" href="<%= account_path(@conn, :index) %>">Accounts</a>
            <a class="pseudo button" href="<%= category_path(@conn, :index) %>">Budgets</a>
            <a class="pseudo button" href="<%= transaction_path(@conn, :index) %>">Transactions</a>
            <a class="pseudo button" href="<%= report_path(@conn, :index) %>">Reports</a>
          </div>
        </nav>
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
